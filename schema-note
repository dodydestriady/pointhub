NOTE DB SCHEMA

Users
  id: string UID
  name: string
  phone_number: string
  image: string
  token: string
  refresh_token: string
  timestamps

Chats
  id: string
  name: string
  image: string
  type: enum
  timestamps

Members
  id: string
  chat_id: string
  user_id: string
  timestamps

Conversations
  id: string
  chat_id: string
  image: string
  message: string
  timestamps
  
reads
  id: id
  readable_id: string
  readable_type: enum
  user_id: string
  read: boolean
  timestamps

